# Module 03 — Prefill Performance

**Status:** Planned

This module will model prompt processing separately from decode. It will cover
operator shapes, attention and MLP work, FLOPs, bytes, arithmetic intensity,
prompt-length scaling, roofline estimates, batching, and profiler validation.

The primary artifact will be `prefill_model.py` plus a predicted-versus-measured
error table across prompt lengths.

